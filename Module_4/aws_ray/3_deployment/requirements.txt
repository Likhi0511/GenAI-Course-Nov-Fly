# =============================================================================
# RAY DOCUMENT PIPELINE — REQUIREMENTS
# =============================================================================
# Strategy: pin only where version conflicts are known, let pip resolve the rest.
# Python 3.9 (Ray 2.9.0 base image)

# ── Ray ───────────────────────────────────────────────────────────────────────
ray[default]==2.9.0
ray[data]==2.9.0

# ── AWS ───────────────────────────────────────────────────────────────────────
# boto3/botocore pinned to match your course environment
boto3==1.42.5

# ── Document processing ───────────────────────────────────────────────────────
# docling 2.69.1 = last release supporting Python 3.9 (2.70.0 dropped it)
docling==2.69.1
python-docx
python-pptx
openpyxl
pypdf
pymupdf
pdf2image
unstructured
beautifulsoup4

# ── AI / ML ───────────────────────────────────────────────────────────────────
openai

# ── Vector database ───────────────────────────────────────────────────────────
# pinecone>=3 uses the new unified package (replaces pinecone-client)
pinecone==8.0.0

# ── Data ─────────────────────────────────────────────────────────────────────
pandas
numpy
scipy
scikit-learn

# ── Images ───────────────────────────────────────────────────────────────────
pillow

# ── Retry / progress / display ───────────────────────────────────────────────
tenacity
tqdm
rich
tabulate          # used by pipeline summary outputs

# ── Logging ──────────────────────────────────────────────────────────────────
structlog         # used by pipeline stages for structured JSON logs

# ── Utilities ────────────────────────────────────────────────────────────────
python-dotenv
pydantic>=2.8.0
pydantic-settings
requests
httpx
aiohttp
